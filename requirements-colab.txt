# Pinned for Google Colab. Notebooks 01-03 run on the free-tier T4; notebook 04 needs an
# A100 or L4 (see the bitsandbytes section at the bottom). All four install from this file.
#
# torch is deliberately NOT pinned: Colab ships a CUDA-matched build, and
# reinstalling it there is slow and frequently breaks the runtime.
# transformers 5.x requires torch>=2.5 and Python>=3.10 — Colab satisfies both.
#
# TRL is used by notebook 03 ONLY, for DPOTrainer. Notebooks 01 and 02 stay on
# plain transformers.Trainer + peft so the masking and packing remain visible on
# the page, and so they are insulated from TRL's ongoing SFTConfig /
# max_seq_length API churn. The DPO loss is a different case: subtle, easy to get
# quietly wrong, and TRL's is the reference implementation. See docs/concepts.md
# section 10.
#
# bitsandbytes is used by notebook 04 ONLY, for 4-bit NF4 weights and the paged
# 8-bit optimizer. Notebooks 01-03 hold the base model in fp16 on purpose: they
# merge an adapter into the base weights between every stage, and merging into a
# quantized base is lossy. Notebook 04 measures how lossy. See section 16.

transformers==5.15.0
peft==0.20.0
datasets==5.0.1
accelerate==1.14.0
trl==0.29.1
bitsandbytes==0.50.2

# --- What DPOConfig changes out from under you ------------------------------
# Read out of trl 0.29.1's source, not recalled. DPOConfig inherits from
# TrainingArguments via trl's BaseConfig, and BOTH layers move defaults.
# Notebook 03 sets every one of these explicitly; if you adapt it, keep doing so.
#
# Changed by trl (BaseConfig):
#   gradient_checkpointing      -> True   (with PEFT this needs
#                                          enable_input_require_grads(), or the
#                                          first backward dies)
#   bf16                        -> None, resolved to `not fp16` in __post_init__
#                                          (so it switches ITSELF on, and a
#                                          Turing T4 then emulates it, slowly)
#   logging_steps               -> 10
#
# Changed by trl (DPOConfig):
#   learning_rate               -> 1e-6   (a full-fine-tune rate, far too low for
#                                          LoRA's ~0.9% of the weights)
#   loss_type                   -> ["sigmoid"], a LIST since trl 0.29. Passing
#                                          the bare string is not the same thing.
#   max_length                  -> 1024, with truncation_mode="keep_start", so
#                                          overflow eats the completion — the
#                                          only part DPO scores
#   beta                        -> 0.1
#
# Inherited unchanged from TrainingArguments, and still worth setting, because
# the defaults suit a different job:
#   per_device_train_batch_size -> 8      (one DPO example is TWO sequences, and
#                                          the reference forward doubles it again)
#   lr_scheduler_type           -> "linear"
#   save_strategy               -> "steps", every 500
#
# Note also that trl's preference collator passes `padding_side` explicitly on
# every call (left for prompts, right for completions), so it neither needs nor
# reads the padding side of the tokenizer you hand it as `processing_class`.
# Notebook 03 still left-pads, for its own batched generation.

# --- bitsandbytes, notebook 04 only -----------------------------------------
# Requires torch>=2.4 (Colab satisfies this) and a CUDA build. Two things about
# it are worth knowing before you copy notebook 04's config elsewhere:
#
#   bnb_4bit_compute_dtype  defaults to torch.float32. That is not the storage
#                           dtype - it is the precision every matmul runs in
#                           after the block is dequantized. Left at the default
#                           you pay fp32 matmul cost for weights that cannot
#                           supply fp32 precision. Notebook 04 sets bfloat16.
#   bnb_4bit_quant_type     defaults to "fp4". "nf4" is strictly better for
#                           normally-distributed weights at the same bit cost.
#
# Also note that p.numel() under-reports on a 4-bit model: Params4bit packs two
# weights per uint8. Notebook 04 counts Linear4bit by its logical shape instead.
#
# The paged optimizers (optim="paged_adamw_8bit") come from here too. With LoRA
# the 8-bit state saves a few hundred MB at most; the paging - spilling to host
# RAM instead of raising CUDA OOM - is the part that earns its place.

# --- Fallback pin set -------------------------------------------------------
# If the pins above ever conflict with Colab's preinstalled torch, this older
# combination is known-good. You must then swap `dtype=` for `torch_dtype=` in
# the model-loading cells (transformers renamed it in v5).
#
# transformers==4.56.2
# peft==0.17.1
# datasets==4.0.0
# accelerate==1.10.1

# --- Colab's preinstalled torchao ------------------------------------------
# Colab ships torchao 0.10.x. peft's optional torchao integration RAISES an
# ImportError rather than degrading when it finds a version below its 0.16.0
# minimum, and the check fires inside get_peft_model(). Nothing in this repo
# uses torchao, so all three notebooks uninstall it:
#
#     %pip uninstall -q -y torchao
